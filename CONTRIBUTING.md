# Contributing

Please keep changes scoped to the `ue-graph-capture` workflow and preserve the
boundary between supported graph capture and unsupported editor surfaces.

Before opening a pull request, validate the skill structure and check that all
examples use a temporary project copy and an output path outside the project.
